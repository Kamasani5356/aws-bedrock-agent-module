Here are the four items rewritten as Jira-ready task descriptions, each broken into 4–5 points:

**Task 1: Remediate AWS Terraform Security Vulnerabilities (Prisma Cloud)**
- Reviewed all outstanding Prisma Cloud alerts flagged across AWS Terraform modules
- Identified root cause of each vulnerability (misconfigured IAM policies, unencrypted resources, open security groups, etc.)
- Updated Terraform modules to apply secure configurations and remediate flagged issues
- Validated fixes by re-scanning with Prisma Cloud to confirm alerts were cleared
- Strengthened overall infrastructure security posture across AWS environment

**Task 2: Secure GitHub Branching Strategy & Automated Testing Pipelines**
- Designed and implemented standardized branch protection rules across all GitHub repositories
- Enforced PR review requirements and status checks before merge to protect main/production branches
- Integrated automated testing pipelines to run on every push/PR for early defect detection
- Standardized workflow across repositories to ensure consistent CI practices
- Documented branching strategy and testing pipeline setup for team reference

**Task 3: Automated GCP Terraform Deployment via Jenkins CI/CD**
- Built Jenkins pipeline to automate Terraform plan/apply for GCP infrastructure
- Integrated Terraform state management and validation steps into the pipeline
- Configured pipeline stages for build, plan approval, and deployment
- Implemented error handling and rollback safeguards for failed deployments
- Reduced manual deployment effort and deployment time for GCP resources

**Task 4: ServiceNow Integration with Jenkins**
- Configured Jenkins to trigger/update ServiceNow change requests as part of pipeline execution
- Set up API/webhook connectivity between Jenkins and ServiceNow
- Mapped pipeline stages to corresponding ServiceNow change management states
- Enabled automated status updates back to ServiceNow tickets on build/deploy success or failure
- Improved traceability and compliance by linking deployments to change records

Want me to format these directly as a Jira-importable CSV or put them in a doc you can copy from?
